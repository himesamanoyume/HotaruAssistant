from packaging.version import parse
import subprocess,tempfile,shutil,psutil,json,sys,time,requests,os,urllib.request,concurrent.futures
from tqdm import tqdm
from urllib.parse import urlparse

class AppUpdate:
    def __init__(self, downloadUrl):
        pass

if __name__ == '__main__':
    pass
    # check_temp_dir()

    # if len(sys.argv) == 2:
    #     update = Update(sys.argv[1])
    # else:
    #     update = Update()

    # update.run()